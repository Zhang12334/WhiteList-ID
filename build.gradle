plugins {
    id 'java'
}

group 'com.yourname'
version '1.0'

repositories {
    mavenCentral()
    maven { url 'https://repo.spigotmc.org/snapshots' }
}

dependencies {
    compile 'org.spigotmc:spigot-api:1.20-R0.1-SNAPSHOT'
}

jar {
    from 'src/main/resources' // 将资源复制到插件 jar 文件中
}
